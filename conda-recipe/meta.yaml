
package:
    name: archiveexport
    version: 0.9

source:    
    path: ..

build:
  number: 1

requirements:
  build:
    - {{ compiler('c') }}
    - make

  host:
    - python
    - xerces-c
    - epics-base

  run:
    - python
    - epics-base

about:
    home: http://www.aps.anl.gov/epics
    license: Epics Open License
    summary: EPICS Base Library
